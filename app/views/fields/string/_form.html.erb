<%#
# String Form Partial

This partial renders an input element for a string attribute.
By default, the input is a text field.

## Local variables:

- `f`:
  A Rails form generator, used to help create the appropriate input fields.
- `field`:
  An instance of [Administrate::Field::String][1].
  A wrapper around the String pulled from the database.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Field/String
%>

<% label_text = t(
  "helpers.label.#{f.object.class.name.underscore}.#{field.attribute}",
  default: field.attribute.to_s,
).titleize %>

<% if field.attribute == :phone && defined?(Phonelib) %>
  <%= f.input field.attribute, label: label_text, input_html: { value: Phonelib.parse(f.object.phone).national } %>
<% else %>
  <%= f.input field.attribute, label: label_text %>
<% end %>

<div class="hint">
  <%= t(
    "helpers.hint.#{f.object.class.name.underscore}.#{field.attribute}",
    default: '',
  ) %>
</div>
